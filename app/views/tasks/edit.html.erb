<h1>id＝<%= @task.id %>のタスク編集ページ</h1>

<%= form_for(@task) do |t| %>
	<%= t.label :content, 'メッセージ' %>
	<%= t.text_field :content %>
	
	<%= t.submit '投稿' %>
<% end %>

<%=link_to '一覧に戻る', tasks_path %>